# Satellite tileset — style probes

Drop ComfyUI style probes here (PNG) for review when iterating on the look (`../../satellite-tileset.md`).
These are **judgement artifacts**, not shipped assets — committed tileset art lives in
`public/tilesets/satellite/` and is listed in `SATELLITE_ASSETS` (`src/ui/tileset.ts`).

Recipe: saved workflow `z_image_pixelart_tile.json` (Z-Image turbo + `pixel_art_style` LoRA +
`SeamlessTile` + `PixelOE`). Prompts + the per-category `tiling` setting are in the plan doc §5.
